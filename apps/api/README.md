# apps/api

The backend API. Owns all database access and business logic (computing gainers/losers, applying ranking thresholds).

Exposes JSON endpoints consumed by `apps/web` today, and by a future mobile app the same way. Not yet scaffolded.
